const valid_tx_stock_class_split = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_stock_class_split

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_stock_class_split;
