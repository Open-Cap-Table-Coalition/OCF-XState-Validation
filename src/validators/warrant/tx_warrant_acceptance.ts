const valid_tx_warrant_acceptance = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_warrant_acceptance

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_warrant_acceptance;
