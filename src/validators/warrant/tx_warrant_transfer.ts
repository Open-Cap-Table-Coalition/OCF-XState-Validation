const valid_tx_warrant_transfer = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_warrant_transfer

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_warrant_transfer;
