const valid_tx_stock_retraction = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_stock_retraction

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_stock_retraction;
