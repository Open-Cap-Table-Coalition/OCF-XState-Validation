const valid_tx_equity_compensation_cancellation = (
  context: any,
  event: any
) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_equity_compensation_cancellation

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_equity_compensation_cancellation;
