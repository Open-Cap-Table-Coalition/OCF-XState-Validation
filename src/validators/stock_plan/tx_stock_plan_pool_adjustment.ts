const valid_tx_stock_plan_pool_adjustment = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_stock_plan_pool_adjustment

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_stock_plan_pool_adjustment;
