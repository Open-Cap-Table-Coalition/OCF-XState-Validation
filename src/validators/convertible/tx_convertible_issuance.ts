const valid_tx_convertible_issuance = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_convertible_issuance

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_convertible_issuance;
