const valid_tx_convertible_cancellation = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_convertible_cancellation

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_convertible_cancellation;
