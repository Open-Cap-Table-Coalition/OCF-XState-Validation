const valid_tx_convertible_retraction = (context: any, event: any) => {
  let valid = false;
  valid = true;
  // TBC: validation of tx_convertible_retraction

  if (valid) {
    return true;
  } else {
    return false;
  }
};

export default valid_tx_convertible_retraction;
