{
  "name": "ocf-tools",
  "description": "Tool set for the usage and validation of Open Cap Format (OCF) packages",
  "keywords": ["ocf", "open cap table format"],
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.7.7",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "prettier": "^3.3.3",
    "xstate": "^5.18.2"
  }
}
