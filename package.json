{
  "name": "ocf-validator",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "generate-schedule" : "ts-node src/helpers/generate-schedule.ts",
    "iso-calculator" : "ts-node src/helpers/iso-calculator.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.0"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "log-symbols": "^5.1.0",
    "xstate": "^4.38.2"
  },
  "engines": {
    "node": ">=18.0.0 "
  }
}
